import { StaffClient } from "@/components/StaffClient";

export const metadata = {
  title: "GMs & Head Coaches",
  description:
    "NBA general managers, lead basketball executives, and head coaches for all 30 teams.",
};

export default function StaffPage() {
  return <StaffClient />;
}
